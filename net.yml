name: Run Shell Code
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run Shell Command
      run: |
        echo "Hello, World!"
        ls -l
        # 这里可以添加更多的Shell命令或调用Shell脚本
